# GemmaGuardian Unified Requirements
# This file contains all dependencies for both Ollama and Transformer modes

# Core dependencies for all modes
opencv-python==4.8.1.78
numpy>=1.21.0
Pillow>=10.0.0
requests>=2.25.0
python-dotenv>=0.19.0
typing-extensions>=4.0.0
dataclasses-json>=0.5.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.6.0
tenacity>=8.0.0
aiohttp>=3.8.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Transformer mode dependencies (optional, installed based on user choice)
# Uncomment these or install separately for transformer mode:
# transformers>=4.53.0
# torch>=2.0.0  # CPU version
# torchvision>=0.15.0
# timm>=1.0.16
# av>=14.4.0

# For GPU support with CUDA (optional):
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
# torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu118

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
